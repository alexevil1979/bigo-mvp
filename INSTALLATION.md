# Инструкция по установке и запуску MVP стримингового сервиса

## 📋 Требования

- Node.js 18+ 
- MongoDB (локально или MongoDB Atlas)
- npm или yarn
- Для мобильного приложения: Android Studio (для Android) или Xcode (для iOS)

## 🚀 Быстрый старт

### 1. Установка зависимостей

#### Backend
```bash
cd backend
npm install
```

#### Web приложение
```bash
cd frontend/web
npm install
```

#### Mobile приложение
```bash
cd frontend/mobile
npm install
```

#### Админ-панель
```bash
cd admin
npm install
```

### 2. Настройка MongoDB

#### Локально
Установите MongoDB и запустите:
```bash
mongod
```

#### MongoDB Atlas (облако)
1. Создайте аккаунт на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте кластер (бесплатный tier доступен)
3. Получите connection string

### 3. Настройка переменных окружения

#### Backend
```bash
cd backend
cp .env.example .env
```

Отредактируйте `.env`:
```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/streaming-mvp
# или для MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/streaming-mvp

JWT_SECRET=your-super-secret-key-change-this
JWT_EXPIRE=7d

# Опционально для платежей
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
```

#### Web приложение
```bash
cd frontend/web
cp .env.example .env.local
```

Отредактируйте `.env.local`:
```env
API_URL=http://localhost:5000
SOCKET_URL=http://localhost:5000
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
```

### 4. Запуск приложений

#### Backend (в отдельном терминале)
```bash
cd backend
npm run dev
```
Сервер будет доступен на `http://localhost:5000`

#### Web приложение (в отдельном терминале)
```bash
cd frontend/web
npm run dev
```
Приложение будет доступно на `http://localhost:3000`

#### Админ-панель (в отдельном терминале)
```bash
cd admin
npm run dev
```
Админка будет доступна на `http://localhost:3001`

#### Mobile приложение

**Для Android:**
```bash
cd frontend/mobile
# Убедитесь, что эмулятор запущен или устройство подключено
npx react-native run-android
```

**Для iOS (только на macOS):**
```bash
cd frontend/mobile
npx react-native run-ios
```

**Важно для мобильного приложения:**
- Измените `API_URL` в `frontend/mobile/src/contexts/AuthContext.js` на IP адрес вашего компьютера для тестирования на реальном устройстве
- Для Android: используйте `adb reverse tcp:5000 tcp:5000` для проброса порта

### 5. Создание первого администратора

Запустите MongoDB и выполните в MongoDB shell или через приложение:
```javascript
use streaming-mvp
db.users.insertOne({
  email: "admin@example.com",
  password: "$2a$10$...", // Хеш пароля (используйте bcrypt)
  nickname: "Admin",
  role: "admin",
  coins: 0,
  beans: 0,
  isActive: true,
  isBanned: false,
  createdAt: new Date()
})
```

Или используйте регистрацию через API и затем обновите роль вручную:
```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"password123","nickname":"Admin"}'
```

Затем в MongoDB:
```javascript
db.users.updateOne(
  { email: "admin@example.com" },
  { $set: { role: "admin" } }
)
```

## 🧪 Тестирование

### 1. Тестирование на Web

1. Откройте `http://localhost:3000`
2. Зарегистрируйтесь
3. Создайте стрим
4. Откройте стрим в другой вкладке браузера
5. Проверьте чат и подарки

### 2. Тестирование на Mobile

1. Убедитесь, что backend запущен
2. Запустите мобильное приложение
3. Зарегистрируйтесь или войдите
4. Создайте стрим или присоединитесь к существующему

### 3. Тестирование админки

1. Откройте `http://localhost:3001`
2. Войдите как администратор
3. Проверьте статистику, управление пользователями и стримами

## 🔧 Решение проблем

### Проблема: MongoDB не подключается
- Убедитесь, что MongoDB запущен
- Проверьте connection string в `.env`
- Проверьте, что порт 27017 не заблокирован

### Проблема: WebRTC не работает
- Убедитесь, что используется HTTPS или localhost
- Проверьте настройки камеры/микрофона в браузере
- Для продакшена настройте TURN сервер

### Проблема: Socket.IO не подключается
- Проверьте, что backend запущен
- Проверьте CORS настройки
- Убедитесь, что порты не заблокированы файрволом

### Проблема: Мобильное приложение не подключается к backend
- Измените `API_URL` на IP адрес вашего компьютера
- Убедитесь, что устройство и компьютер в одной сети
- Для Android используйте `adb reverse`

## 📝 Дополнительные настройки

### Настройка Stripe (опционально)

1. Создайте аккаунт на [Stripe](https://stripe.com)
2. Получите тестовые ключи
3. Добавьте их в `.env` файл backend
4. Настройте webhook endpoint: `http://localhost:5000/api/payments/stripe-webhook`

### Настройка Cloudinary для аватаров (опционально)

1. Создайте аккаунт на [Cloudinary](https://cloudinary.com)
2. Получите credentials
3. Добавьте их в `.env` файл backend

## 🎯 Следующие шаги

После успешного запуска MVP:

1. Настройте продакшен окружение
2. Настройте HTTPS
3. Настройте TURN сервер для WebRTC
4. Добавьте мониторинг и логирование
5. Оптимизируйте производительность
6. Добавьте тесты

## 📞 Поддержка

При возникновении проблем проверьте:
- Логи backend сервера
- Консоль браузера (F12)
- Логи мобильного приложения (React Native Debugger)

