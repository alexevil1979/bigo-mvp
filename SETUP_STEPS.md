# Пошаговая инструкция по настройке нового функционала

## ✅ Что уже сделано:
- ✅ Компонент QRCodeLogin создан
- ✅ Контроллер qrController.js создан
- ✅ Маршруты добавлены в auth.js
- ✅ Пакет qrcode установлен в backend
- ✅ Страницы логина и регистрации обновлены
- ✅ Страница трансляции обновлена в стиле Bigo Live

---

## 📋 ШАГ 1: Установка зависимостей (если еще не установлены)

### На бэкенде:
```bash
cd backend
npm install
```

**Проверьте, что установлены:**
- `qrcode` (уже есть в package.json)
- Все остальные зависимости

### На фронтенде:
```bash
cd frontend/web
npm install
```

**Проверьте, что установлены:**
- `socket.io-client` (должен быть)
- `axios` (должен быть)

---

## 📋 ШАГ 2: Проверка подключения qrController к серверу

### Проверьте файл `backend/routes/auth.js`:

Должны быть строки:
```javascript
const qrController = require('../controllers/qrController');
// ...
router.post('/qr-session', qrController.createQRSession);
router.post('/qr-confirm', qrController.confirmQRLogin);
router.get('/qr-session/:sessionId', qrController.checkQRSession);
```

### Проверьте файл `backend/server.js`:

Должна быть строка:
```javascript
app.set('io', io); // Для доступа к Socket.IO в контроллерах
```

**Если этой строки нет, добавьте после создания `io`:**
```javascript
// После строки: const io = socketIo(server, {...});
app.set('io', io);
```

---

## 📋 ШАГ 3: Обновление server.js для доступа к Socket.IO в контроллерах

Откройте `backend/server.js` и найдите место, где создается `io`:

```javascript
const io = socketIo(server, {
  cors: {
    origin: process.env.CORS_ORIGIN?.split(',') || '*',
    methods: ['GET', 'POST'],
    credentials: true
  }
});
```

**Добавьте сразу после этого:**
```javascript
// Делаем io доступным для контроллеров
app.set('io', io);
```

---

## 📋 ШАГ 4: Перезапуск бэкенда

### Если используете PM2:
```bash
cd backend
pm2 restart bigo-backend
# или
pm2 restart all
```

### Если запускаете вручную:
```bash
cd backend
# Остановите текущий процесс (Ctrl+C)
npm run dev
# или
npm start
```

---

## 📋 ШАГ 5: Проверка работы API

### Тест 1: Создание QR-сессии

Откройте браузер или используйте curl:

```bash
curl -X POST http://localhost:5000/api/auth/qr-session
```

**Ожидаемый ответ:**
```json
{
  "sessionId": "abc123...",
  "qrData": "data:image/png;base64,iVBORw0KG...",
  "expiresIn": 300
}
```

### Тест 2: Проверка страницы логина

1. Откройте `http://localhost:3000/login` (или ваш URL фронтенда)
2. Должен отображаться QR-код
3. Должны быть видны кнопки социальных сетей
4. Должна быть форма входа

---

## 📋 ШАГ 6: Настройка OAuth (опционально, для полной функциональности)

### Для каждого провайдера нужно:

1. **Создать приложение в социальной сети:**
   - Facebook: https://developers.facebook.com/
   - Google: https://console.cloud.google.com/
   - VK: https://vk.com/apps?act=manage
   - Twitter: https://developer.twitter.com/

2. **Получить Client ID и Client Secret**

3. **Добавить в `backend/.env`:**
```env
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
VK_APP_ID=your_vk_app_id
VK_APP_SECRET=your_vk_app_secret
TWITTER_CONSUMER_KEY=your_twitter_key
TWITTER_CONSUMER_SECRET=your_twitter_secret
```

4. **Установить Passport пакеты:**
```bash
cd backend
npm install passport passport-facebook passport-google-oauth20 passport-vkontakte passport-twitter
```

5. **Создать `backend/controllers/oauthController.js`** (если еще не создан)

**Примечание:** OAuth можно настроить позже, основная функциональность (QR-код, форма входа) работает без него.

---

## 📋 ШАГ 7: Проверка страницы трансляции

1. Откройте любой активный стрим: `http://localhost:3000/stream/[id]`
2. Проверьте элементы:
   - ✅ Верхняя панель с профилем стримера
   - ✅ Кнопки "Поделиться" и "Подписаться"
   - ✅ Видеоплеер с градиентом
   - ✅ Нижняя панель с чатом
   - ✅ Баннер с рекламой
   - ✅ Боковая панель с чатом и подарками

---

## 📋 ШАГ 8: Тестирование QR-кода (требует мобильное приложение)

**Для полного тестирования QR-кода нужно:**

1. **На веб-сайте:**
   - Откройте `/login`
   - Должен отобразиться QR-код

2. **В мобильном приложении (когда будет готово):**
   - Откройте функцию "Сканировать QR-код"
   - Отсканируйте QR-код с веб-страницы
   - Мобильное приложение должно отправить запрос на `/api/auth/qr-confirm`
   - Веб-страница должна автоматически авторизовать пользователя

**Для тестирования без мобильного приложения:**
- QR-код генерируется и отображается
- API endpoints работают
- Socket.IO подключение устанавливается

---

## 🔧 Возможные проблемы и решения

### Проблема 1: "Cannot find module '../controllers/qrController'"

**Решение:**
```bash
cd backend
# Проверьте, что файл существует
ls controllers/qrController.js
# Если нет - проверьте путь в routes/auth.js
```

### Проблема 2: "io is not defined" в qrController

**Решение:**
Добавьте в `backend/server.js` после создания `io`:
```javascript
app.set('io', io);
```

### Проблема 3: QR-код не генерируется

**Решение:**
1. Проверьте, что бэкенд запущен
2. Проверьте консоль браузера на ошибки
3. Проверьте Network tab в DevTools - должен быть запрос на `/api/auth/qr-session`
4. Проверьте логи бэкенда

### Проблема 4: CORS ошибки

**Решение:**
Убедитесь, что в `backend/.env` указан правильный `CORS_ORIGIN`:
```env
CORS_ORIGIN=http://localhost:3000,https://your-frontend-url.com
```

---

## ✅ Чеклист готовности

- [ ] Зависимости установлены (`npm install` в backend и frontend)
- [ ] `app.set('io', io)` добавлено в server.js
- [ ] Бэкенд перезапущен
- [ ] API `/api/auth/qr-session` отвечает
- [ ] Страница `/login` отображает QR-код
- [ ] Страница `/register` обновлена
- [ ] Страница трансляции `/stream/[id]` обновлена
- [ ] OAuth настроен (опционально)

---

## 🚀 Следующие шаги (опционально)

1. **Реализовать OAuth контроллер** для полной функциональности социальных сетей
2. **Добавить мобильное приложение** для сканирования QR-кода
3. **Настроить Redis** для хранения QR-сессий (вместо in-memory)
4. **Добавить аналитику** использования QR-кода vs OAuth

---

## 📞 Если что-то не работает

1. Проверьте логи бэкенда: `pm2 logs bigo-backend`
2. Проверьте консоль браузера (F12)
3. Проверьте Network tab в DevTools
4. Убедитесь, что все переменные окружения установлены



