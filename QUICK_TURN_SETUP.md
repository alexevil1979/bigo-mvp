# Быстрая установка TURN сервера

## 🚀 Установка на сервере

### 1. Обновите код

```bash
cd /ssd/www/bigo-mvp
git pull origin master
```

### 2. Запустите скрипт установки

```bash
cd backend/scripts
chmod +x setup-turn.sh
sudo ./setup-turn.sh
```

Скрипт автоматически:
- ✅ Установит coturn
- ✅ Сгенерирует секретный ключ
- ✅ Настроит конфигурацию
- ✅ Откроет порты в файрволе
- ✅ Запустит сервис

**Важно:** Скопируйте секретный ключ, который выведет скрипт!

### 3. Настройте фронтенд

Создайте или отредактируйте файл `frontend/web/.env.local`:

```env
NEXT_PUBLIC_WEBRTC_TURN_SERVER=turn:bigo.1tlt.ru:3478
NEXT_PUBLIC_WEBRTC_TURN_SECRET=ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_ИЗ_СКРИПТА
```

### 4. Пересоберите и перезапустите фронтенд

```bash
cd /ssd/www/bigo-mvp/frontend/web
npm run build
pm2 restart nio-frontend
```

### 5. Проверьте работу

```bash
# Проверка статуса TURN сервера
sudo systemctl status coturn

# Просмотр логов
sudo tail -f /var/log/turnserver.log
```

## ✅ Готово!

После этого стримы должны работать на мобильных устройствах.

Подробная инструкция: `INSTALL_TURN_SERVER.md`



