# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Модуль express не найден

## Проблема
После удаления `node_modules` зависимости не были переустановлены. Сервер не может запуститься.

## Решение: Полная переустановка зависимостей

Выполните на сервере **ПОСЛЕДОВАТЕЛЬНО** все команды:

```bash
# 1. Остановите бэкенд
cd /ssd/www/bigo-mvp/backend
pm2 stop bigo-backend

# 2. Убедитесь, что вы в правильной директории
pwd
# Должно быть: /ssd/www/bigo-mvp/backend

# 3. Проверьте наличие package.json
ls -la package.json

# 4. Удалите node_modules и package-lock.json (если есть)
rm -rf node_modules
rm -f package-lock.json

# 5. Очистите кэш npm
npm cache clean --force

# 6. Переустановите ВСЕ зависимости
npm install

# 7. Проверьте, что express установлен
npm list express

# 8. Если express не установлен, установите явно
npm install express --save

# 9. Проверьте установку всех основных модулей
npm list express mongoose socket.io cors dotenv

# 10. Запустите бэкенд
pm2 start bigo-backend --update-env

# 11. Проверьте логи (должны быть только успешные сообщения)
pm2 logs bigo-backend --lines 20
```

## Если проблема сохраняется

Проверьте версию Node.js:

```bash
node --version
# Должна быть 18.x или выше

npm --version
# Должна быть 9.x или выше
```

Если версия Node.js старая, обновите её или используйте nvm:

```bash
# Установка через nvm (если установлен)
nvm install 18
nvm use 18

# Затем повторите npm install
npm install
```

## Альтернативное решение

Если ничего не помогает, попробуйте установить зависимости вручную:

```bash
cd /ssd/www/bigo-mvp/backend
npm install express mongoose socket.io cors dotenv jsonwebtoken bcryptjs express-validator multer qrcode uuid whatwg-url
pm2 restart bigo-backend --update-env
```

## Проверка после исправления

После успешной установки вы должны увидеть в логах:

```
🚀 Сервер запущен на порту 5000
📡 WebSocket сервер готов для подключений
✅ MongoDB подключена
```

**БЕЗ ошибок** `Cannot find module`.

