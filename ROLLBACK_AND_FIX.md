# 🔄 Откат на рабочий коммит и исправление проблем

## Что было сделано:

1. ✅ Откатился на последний успешный коммит `b76e931` (2 часа назад)
2. ✅ Запушил изменения с `--force` для восстановления рабочего состояния

## Текущее состояние:

- Сайт должен вернуться к рабочему состоянию
- Все изменения после коммита `b76e931` отменены
- Vercel должен автоматически задеплоить рабочую версию

## Что нужно сделать дальше:

### Шаг 1: Проверь деплой на Vercel

1. Зайди на [vercel.com](https://vercel.com)
2. Проверь, что появился новый деплой
3. Должен быть успешный деплой с коммитом `b76e931`

### Шаг 2: После успешного деплоя - добавим изменения по одному

Чтобы найти проблему, нужно добавлять изменения постепенно:

#### Изменение 1: Добавим favicon.ico

```bash
# Убедись, что файл существует
# frontend/web/public/favicon.ico

# Добавь файл в git
git add frontend/web/public/favicon.ico
git commit -m "Добавлен favicon.ico"
git push origin master
```

**Проверь:** Деплой должен пройти успешно. Если нет - проблема в favicon.ico.

#### Изменение 2: Добавим ссылки на favicon в код

```bash
# Добавь favicon в _app.js
# (уже сделано в коде, но нужно проверить)

git add frontend/web/pages/_app.js
git commit -m "Добавлен favicon в _app.js"
git push origin master
```

#### Изменение 3: Добавим favicon в overlay видео

```bash
# Добавь favicon в StreamBroadcaster и stream/[id].js
git add frontend/web/components/StreamBroadcaster.js frontend/web/pages/stream/[id].js
git commit -m "Добавлен favicon в overlay видео"
git push origin master
```

### Шаг 3: Если какое-то изменение ломает деплой

1. Открой логи ошибки на Vercel
2. Скопируй полный текст ошибки
3. Отправь мне - я исправлю проблему

---

## 🔍 Диагностика проблем:

### Проблема: favicon.ico не загружается

**Возможные причины:**
1. Файл не добавлен в git
2. Файл слишком большой (> 100KB)
3. Неправильный путь к файлу

**Решение:**
- Проверь: `git ls-files | grep favicon.ico`
- Если файла нет: `git add frontend/web/public/favicon.ico`
- Проверь размер файла

### Проблема: Ошибка сборки Next.js

**Возможные причины:**
1. Синтаксическая ошибка в коде
2. Неправильный импорт
3. Отсутствующая зависимость

**Решение:**
- Проверь логи сборки на Vercel
- Попробуй собрать локально: `cd frontend/web && npm run build`

---

## 📝 Чеклист:

- [ ] Сайт вернулся к рабочему состоянию
- [ ] Новый деплой на Vercel успешен
- [ ] Добавлен favicon.ico в git
- [ ] Добавлены ссылки на favicon в код
- [ ] Все деплои проходят успешно

---

## ⚠️ ВАЖНО:

**НЕ ДЕЛАЙ МНОЖЕСТВЕННЫХ ИЗМЕНЕНИЙ ОДНОВРЕМЕННО!**

Добавляй изменения по одному и проверяй деплой после каждого изменения. Это поможет быстро найти проблемный коммит.



