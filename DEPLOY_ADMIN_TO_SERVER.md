# Пошаговая инструкция: Деплой админ-панели на локальный сервер

## 📋 Предварительные требования

- Сервер с установленным Node.js 18+ (см. инструкцию по обновлению в `DEPLOY_FRONTEND_TO_SERVER.md`)
- PM2 установлен глобально
- Apache настроен и работает
- Домен или поддомен для админки (например, `admin.bigo.1tlt.ru`)

---

## Шаг 1: Подготовка на сервере

### 1.1. Перейдите в директорию проекта

```bash
cd /ssd/www/bigo-mvp
```

### 1.2. Обновите код из репозитория

```bash
git pull origin master
```

---

## Шаг 2: Установка зависимостей админки

### 2.1. Перейдите в директорию админки

```bash
cd admin
```

### 2.2. Установите зависимости

```bash
npm install
```

**Важно:** Убедитесь, что Node.js версии 18+:
```bash
node -v
# Должно быть: v18.x.x или выше
```

---

## Шаг 3: Настройка переменных окружения

### 3.1. Создайте файл `.env.local` в `admin/`

```bash
nano .env.local
```

### 3.2. Добавьте следующие переменные:

```env
# API URL (замените на ваш домен)
API_URL=https://api.bigo.1tlt.ru
# или если без SSL:
# API_URL=http://api.bigo.1tlt.ru

# Порт для админки (по умолчанию 3001, чтобы не конфликтовать с основным сайтом)
PORT=3001
```

### 3.3. Сохраните файл (Ctrl+O, Enter, Ctrl+X)

---

## Шаг 4: Сборка проекта

### 4.1. Соберите production версию

```bash
npm run build
```

**Примечание:** Сборка может занять 1-3 минуты.

### 4.2. Проверьте, что сборка прошла успешно

Должна появиться папка `.next` с собранными файлами.

---

## Шаг 5: Настройка PM2 для админки

### 5.1. Создайте конфигурационный файл PM2

В директории `admin/` создайте файл `ecosystem.config.js`:

```bash
nano ecosystem.config.js
```

### 5.2. Добавьте следующую конфигурацию:

```javascript
module.exports = {
  apps: [{
    name: 'nio-admin',
    script: 'npm',
    args: 'start',
    cwd: '/ssd/www/bigo-mvp/admin',
    instances: 1,
    exec_mode: 'fork',
    env: {
      NODE_ENV: 'production',
      PORT: 3001
    },
    error_file: '/root/.pm2/logs/nio-admin-error.log',
    out_file: '/root/.pm2/logs/nio-admin-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_memory_restart: '500M'
  }]
};
```

**Важно:** Замените путь `/ssd/www/bigo-mvp/admin` на ваш реальный путь.

### 5.3. Сохраните файл

---

## Шаг 6: Запуск через PM2

### 6.1. Запустите админку

```bash
pm2 start ecosystem.config.js
```

### 6.2. Проверьте статус

```bash
pm2 status
```

Должен появиться процесс `nio-admin` со статусом `online`.

### 6.3. Сохраните конфигурацию PM2

```bash
pm2 save
```

---

## Шаг 7: Настройка Apache для админки

### 7.1. Включите необходимые модули (если еще не включены)

```bash
sudo a2enmod proxy
sudo a2enmod proxy_http
sudo a2enmod rewrite
sudo a2enmod headers
sudo a2enmod ssl

sudo systemctl restart apache2
```

### 7.2. Создайте конфигурационный файл Apache для админки

```bash
sudo nano /etc/apache2/sites-available/bigo-admin.conf
```

### 7.3. Добавьте конфигурацию:

```apache
<VirtualHost *:80>
    ServerName admin.bigo.1tlt.ru
    
    # Логи
    ErrorLog ${APACHE_LOG_DIR}/bigo-admin-error.log
    CustomLog ${APACHE_LOG_DIR}/bigo-admin-access.log combined
    
    # Редирект на HTTPS (если используете SSL) - раскомментируйте:
    # Redirect permanent / https://admin.bigo.1tlt.ru/
    
    # Если без SSL, используйте эту секцию:
    ProxyPreserveHost On
    ProxyRequests Off
    
    # Основной прокси для админки Next.js
    ProxyPass / http://localhost:3001/
    ProxyPassReverse / http://localhost:3001/
    
    # Заголовки для правильной работы прокси
    RequestHeader set X-Forwarded-Proto "http"
    RequestHeader set X-Forwarded-Port "80"
    RequestHeader set X-Real-IP %{REMOTE_ADDR}s
</VirtualHost>

# Если используете HTTPS:
<VirtualHost *:443>
    ServerName admin.bigo.1tlt.ru
    
    # Логи
    ErrorLog ${APACHE_LOG_DIR}/bigo-admin-ssl-error.log
    CustomLog ${APACHE_LOG_DIR}/bigo-admin-ssl-access.log combined
    
    # SSL настройки
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/admin.bigo.1tlt.ru/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/admin.bigo.1tlt.ru/privkey.pem
    
    # SSL Security
    SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1
    SSLCipherSuite ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384
    SSLHonorCipherOrder off
    
    # Proxy настройки
    ProxyPreserveHost On
    ProxyRequests Off
    
    ProxyPass / http://localhost:3001/
    ProxyPassReverse / http://localhost:3001/
    
    # Заголовки для HTTPS
    RequestHeader set X-Forwarded-Proto "https"
    RequestHeader set X-Forwarded-Port "443"
    RequestHeader set X-Real-IP %{REMOTE_ADDR}s
</VirtualHost>
```

**Важно:** Замените `admin.bigo.1tlt.ru` на ваш домен для админки.

### 7.4. Активируйте сайт

```bash
sudo a2ensite bigo-admin.conf
```

### 7.5. Проверьте конфигурацию

```bash
sudo apache2ctl configtest
```

Должно быть: `Syntax OK`

### 7.6. Перезагрузите Apache

```bash
sudo systemctl reload apache2
```

---

## Шаг 8: Настройка DNS

### 8.1. Добавьте A-запись для поддомена админки

В настройках DNS вашего домена добавьте:
- **Тип:** A
- **Имя:** `admin` (или `admin.bigo.1tlt.ru`)
- **Значение:** IP адрес вашего сервера
- **TTL:** 3600 (или по умолчанию)

### 8.2. Дождитесь распространения DNS (обычно 5-30 минут)

Проверьте:
```bash
ping admin.bigo.1tlt.ru
```

---

## Шаг 9: Настройка SSL для админки

### 9.1. Установите Certbot (если еще не установлен)

```bash
sudo apt-get update
sudo apt-get install certbot python3-certbot-apache
```

### 9.2. Получите SSL-сертификат для админки

```bash
sudo certbot --apache -d admin.bigo.1tlt.ru
```

Certbot автоматически:
- Получит SSL-сертификат
- Настроит конфигурацию Apache для HTTPS
- Добавит редирект с HTTP на HTTPS

### 9.3. Проверьте конфигурацию

```bash
sudo apache2ctl configtest
sudo systemctl reload apache2
```

---

## Шаг 10: Проверка работы

### 10.1. Проверьте, что админка запущена

```bash
pm2 logs nio-admin --lines 50
```

Должно быть что-то вроде:
```
ready - started server on 0.0.0.0:3001
```

### 10.2. Проверьте доступность через локальный порт

```bash
curl http://localhost:3001
```

Должен вернуться HTML код страницы админки.

### 10.3. Проверьте через домен

Откройте в браузере: `https://admin.bigo.1tlt.ru`

Должна открыться страница входа в админку.

---

## Шаг 11: Создание первого администратора

### 11.1. Зарегистрируйтесь через основной сайт

1. Откройте основной сайт: `https://bigo.1tlt.ru`
2. Зарегистрируйтесь с email и паролем
3. **Запомните email и пароль!**

### 11.2. Сделайте пользователя администратором

**Вариант A: Через MongoDB Shell**

```bash
# Подключитесь к MongoDB
mongosh
# или для старых версий: mongo
```

```javascript
// Выберите базу данных
use streaming-mvp

// Найдите пользователя по email и установите роль admin
db.users.updateOne(
  { email: "ваш-email@example.com" },
  { $set: { role: "admin" } }
)

// Проверьте
db.users.findOne({ email: "ваш-email@example.com" })
```

**Вариант B: Через MongoDB Compass**

1. Откройте MongoDB Compass
2. Подключитесь к базе данных
3. Выберите базу: `streaming-mvp`
4. Откройте коллекцию: `users`
5. Найдите пользователя по email
6. Измените поле `role` с `"user"` на `"admin"`
7. Сохраните изменения

### 11.3. Войдите в админку

1. Откройте `https://admin.bigo.1tlt.ru`
2. Введите email и пароль
3. Должна открыться админ-панель

---

## Шаг 12: Автоматическое обновление (опционально)

### 12.1. Создайте скрипт для автоматического обновления

```bash
nano /ssd/www/bigo-mvp/deploy-admin.sh
```

### 12.2. Добавьте содержимое:

```bash
#!/bin/bash

cd /ssd/www/bigo-mvp
git pull origin master

cd admin
npm install
npm run build

pm2 restart nio-admin

echo "Admin panel deployed successfully!"
```

### 12.3. Сделайте скрипт исполняемым

```bash
chmod +x /ssd/www/bigo-mvp/deploy-admin.sh
```

### 12.4. Запускайте при необходимости:

```bash
/ssd/www/bigo-mvp/deploy-admin.sh
```

---

## 🔧 Полезные команды PM2 для админки

```bash
# Просмотр логов
pm2 logs nio-admin

# Перезапуск
pm2 restart nio-admin

# Остановка
pm2 stop nio-admin

# Удаление из PM2
pm2 delete nio-admin

# Мониторинг
pm2 monit
```

---

## 🐛 Решение проблем

### Проблема: Админка не запускается

**Решение:**
1. Проверьте логи: `pm2 logs nio-admin --err`
2. Убедитесь, что порт 3001 свободен: `netstat -tulpn | grep 3001`
3. Проверьте переменные окружения в `.env.local`
4. Убедитесь, что Node.js версии 18+: `node -v`

### Проблема: Ошибка "Cannot find module"

**Решение:**
```bash
cd /ssd/www/bigo-mvp/admin
rm -rf node_modules package-lock.json
npm install
npm run build
pm2 restart nio-admin
```

### Проблема: Apache не проксирует запросы

**Решение:**
1. Проверьте конфигурацию: `sudo apache2ctl configtest`
2. Проверьте, что админка запущена: `pm2 status`
3. Проверьте логи Apache: `sudo tail -f /var/log/apache2/bigo-admin-error.log`

### Проблема: "Доступ только для администраторов"

**Решение:**
1. Убедитесь, что пользователь имеет роль `admin` в MongoDB
2. Проверьте в MongoDB:
   ```javascript
   db.users.findOne({ email: "ваш-email@example.com" })
   // Должно быть: role: "admin"
   ```

### Проблема: SSL-сертификат не работает

**Решение:**
1. Убедитесь, что DNS запись для `admin.bigo.1tlt.ru` настроена
2. Проверьте сертификат: `sudo certbot certificates`
3. Попробуйте получить сертификат заново: `sudo certbot --apache -d admin.bigo.1tlt.ru --force-renewal`

---

## ✅ Готово!

После выполнения всех шагов админ-панель будет доступна по адресу:
- **HTTP:** `http://admin.bigo.1tlt.ru`
- **HTTPS:** `https://admin.bigo.1tlt.ru` (после настройки SSL)

**Важно:** Не забудьте создать первого администратора через MongoDB!

---

## 📝 Итоговая структура на сервере

После деплоя у вас будет:

1. **Основной сайт:** `bigo.1tlt.ru` → порт 3000 → PM2 процесс `nio-frontend`
2. **Админ-панель:** `admin.bigo.1tlt.ru` → порт 3001 → PM2 процесс `nio-admin`
3. **API:** `api.bigo.1tlt.ru` → порт 5000 → PM2 процесс `bigo-backend`

Все три приложения работают независимо и могут обновляться отдельно.



