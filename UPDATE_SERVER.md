# Полная инструкция по обновлению на сервере

## Обновление фронтенда

```bash
# 1. Перейдите в корневую директорию проекта
cd /ssd/www/bigo-mvp

# 2. Получите последние изменения из Git
git pull origin master

# 3. Перейдите в директорию фронтенда
cd frontend/web

# 4. Остановите PM2 процесс (если нужно)
pm2 stop nio-frontend

# 5. Удалите старую сборку
rm -rf .next

# 6. Пересоберите проект
npm run build

# 7. Запустите PM2 процесс снова
pm2 restart nio-frontend

# 8. Проверьте статус
pm2 status

# 9. Проверьте логи
pm2 logs nio-frontend --lines 30
```

## Если нужно переустановить зависимости

```bash
# 1. Перейдите в директорию фронтенда
cd /ssd/www/bigo-mvp/frontend/web

# 2. Остановите PM2
pm2 stop nio-frontend

# 3. Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# 4. Переустановите зависимости
npm install

# 5. Удалите старую сборку
rm -rf .next

# 6. Пересоберите проект
npm run build

# 7. Запустите PM2 процесс снова
pm2 restart nio-frontend

# 8. Сохраните конфигурацию PM2
pm2 save

# 9. Проверьте статус
pm2 status

# 10. Проверьте логи
pm2 logs nio-frontend --lines 30
```

## Обновление бэкенда

```bash
# 1. Перейдите в корневую директорию проекта
cd /ssd/www/bigo-mvp

# 2. Получите последние изменения из Git
git pull origin master

# 3. Перейдите в директорию бэкенда
cd backend

# 4. Остановите PM2 процесс
pm2 stop bigo-backend

# 5. Переустановите зависимости (если нужно)
npm install

# 6. Запустите PM2 процесс снова
pm2 restart bigo-backend

# 7. Проверьте статус
pm2 status

# 8. Проверьте логи
pm2 logs bigo-backend --lines 30
```

## Обновление админ-панели

```bash
# 1. Перейдите в корневую директорию проекта
cd /ssd/www/bigo-mvp

# 2. Получите последние изменения из Git
git pull origin master

# 3. Перейдите в директорию админ-панели
cd admin

# 4. Остановите PM2 процесс
pm2 stop nio-admin

# 5. Удалите старую сборку
rm -rf .next

# 6. Переустановите зависимости (если нужно)
npm install

# 7. Пересоберите проект
npm run build

# 8. Запустите PM2 процесс снова
pm2 restart nio-admin

# 9. Проверьте статус
pm2 status

# 10. Проверьте логи
pm2 logs nio-admin --lines 30
```

## Полное обновление всего проекта

```bash
# 1. Перейдите в корневую директорию проекта
cd /ssd/www/bigo-mvp

# 2. Получите последние изменения из Git
git pull origin master

# 3. Обновите фронтенд
cd frontend/web
pm2 stop nio-frontend
rm -rf .next
npm run build
pm2 restart nio-frontend

# 4. Обновите бэкенд
cd ../../backend
pm2 stop bigo-backend
npm install
pm2 restart bigo-backend

# 5. Обновите админ-панель (если используется)
cd ../admin
pm2 stop nio-admin
rm -rf .next
npm run build
pm2 restart nio-admin

# 6. Вернитесь в корневую директорию
cd /ssd/www/bigo-mvp

# 7. Сохраните конфигурацию PM2
pm2 save

# 8. Проверьте статус всех процессов
pm2 status

# 9. Проверьте логи всех процессов
pm2 logs --lines 20
```

## Проверка после обновления

```bash
# 1. Проверьте статус всех PM2 процессов
pm2 status

# Все процессы должны быть в статусе "online"

# 2. Проверьте логи фронтенда
pm2 logs nio-frontend --lines 20

# Должно быть:
# ✓ Ready in Xms
# - Local: http://localhost:3000
# НЕ должно быть ошибок

# 3. Проверьте логи бэкенда
pm2 logs bigo-backend --lines 20

# Должно быть:
# 🚀 Сервер запущен на порту 5000
# ✅ MongoDB подключена
# НЕ должно быть ошибок

# 4. Проверьте локальный доступ к фронтенду
curl http://localhost:3000

# Должен вернуть HTML страницу

# 5. Проверьте локальный доступ к бэкенду
curl http://localhost:5000/api/streams

# Должен вернуть JSON с данными стримов

# 6. Проверьте сайт в браузере
# Откройте https://bigo.1tlt.ru
# Сайт должен загружаться без ошибок
```

## Если что-то пошло не так

### Откат изменений

```bash
# 1. Перейдите в корневую директорию проекта
cd /ssd/www/bigo-mvp

# 2. Посмотрите историю коммитов
git log --oneline -10

# 3. Откатитесь на предыдущий коммит (замените COMMIT_HASH на нужный)
git reset --hard COMMIT_HASH

# 4. Обновите фронтенд
cd frontend/web
rm -rf .next
npm run build
pm2 restart nio-frontend

# 5. Обновите бэкенд
cd ../../backend
pm2 restart bigo-backend
```

### Проверка ошибок

```bash
# 1. Проверьте логи PM2
pm2 logs --lines 50

# 2. Проверьте логи Apache
sudo tail -f /var/log/apache2/error.log

# 3. Проверьте логи TURN сервера (если используется)
sudo tail -f /var/log/turnserver/turnserver.log

# 4. Проверьте использование ресурсов
free -h
df -h
top
```

## Готово!

После выполнения всех шагов проект должен быть обновлен и работать корректно.

