# 🚀 Как задеплоить изменения на сайт

## Проблема: Изменения не видны на сайте

**Причина:** Изменения сделаны локально, но не задеплоены на Vercel.

---

## ✅ РЕШЕНИЕ: Деплой изменений на Vercel

### Вариант 1: Через Git (рекомендуется)

#### ШАГ 1: Сохранить изменения в Git

```bash
# Перейди в корень проекта
cd C:\Users\1\Documents\bigo

# Проверь статус
git status

# Добавь все изменения
git add .

# Создай коммит
git commit -m "Добавлен QR-код, обновлен дизайн страниц в стиле Bigo Live"

# Отправь на GitHub
git push origin main
```

#### ШАГ 2: Vercel автоматически задеплоит

Если проект подключен к Vercel через GitHub:
- Vercel автоматически обнаружит изменения
- Начнется автоматическая сборка
- Через 1-3 минуты изменения появятся на сайте

**Проверь деплой:**
1. Зайди на [vercel.com](https://vercel.com)
2. Выбери проект `bigo-web` (или как называется твой проект)
3. Проверь вкладку **"Deployments"**
4. Должен быть новый деплой со статусом **"Building"** или **"Ready"**

---

### Вариант 2: Через Vercel CLI (если Git не настроен)

#### ШАГ 1: Установить Vercel CLI

```bash
npm install -g vercel
```

#### ШАГ 2: Войти в Vercel

```bash
cd C:\Users\1\Documents\bigo\frontend\web
vercel login
```

#### ШАГ 3: Задеплоить

```bash
# Если проект уже подключен
vercel --prod

# Или если первый раз
vercel
# Следуй инструкциям:
# - Set up and deploy? Y
# - Which scope? (выбери свой аккаунт)
# - Link to existing project? Y (если проект уже есть)
# - What's the name of your project? bigo-web
# - In which directory is your code located? ./
```

---

### Вариант 3: Ручной деплой через Vercel Dashboard

1. Зайди на [vercel.com](https://vercel.com)
2. Выбери проект `bigo-web`
3. Нажми **"Settings" → "Git"**
4. Если проект подключен к GitHub:
   - Нажми **"Redeploy"** на последнем деплое
   - Или нажми **"Deploy"** → выбери последний коммит

---

## 🔍 Проверка после деплоя

### 1. Проверь страницу логина:

Открой: `https://твой-vercel-url.vercel.app/login`

**Должно быть видно:**
- ✅ QR-код для входа
- ✅ Кнопки социальных сетей (Facebook, Google, VK, Twitter)
- ✅ Форма входа
- ✅ Новый дизайн

### 2. Проверь страницу регистрации:

Открой: `https://твой-vercel-url.vercel.app/register`

**Должно быть видно:**
- ✅ Кнопки социальных сетей
- ✅ Форма регистрации
- ✅ Чекбокс согласия с условиями
- ✅ Новый дизайн

### 3. Проверь страницу трансляции:

Открой любой активный стрим

**Должно быть видно:**
- ✅ Верхняя панель с профилем стримера
- ✅ Кнопки "Поделиться" и "Подписаться"
- ✅ Видеоплеер с градиентом
- ✅ Нижняя панель с чатом
- ✅ Баннер с рекламой

---

## ⚠️ Если изменения все еще не видны

### 1. Очисти кэш браузера:

- **Chrome/Edge:** `Ctrl + Shift + Delete` → выбери "Кэшированные изображения и файлы"
- Или открой в режиме инкогнито: `Ctrl + Shift + N`

### 2. Проверь, что деплой завершился:

- Зайди на Vercel Dashboard
- Проверь статус последнего деплоя
- Если есть ошибки - проверь логи сборки

### 3. Проверь переменные окружения:

В Vercel Dashboard → Settings → Environment Variables должны быть:
- `NEXT_PUBLIC_API_URL` = `https://api.bigo.1tlt.ru`
- `NEXT_PUBLIC_SOCKET_URL` = `https://api.bigo.1tlt.ru`

### 4. Проверь консоль браузера:

- Нажми `F12`
- Вкладка **Console**
- Ищи ошибки (красные сообщения)

---

## 📝 Быстрая проверка Git

```bash
cd C:\Users\1\Documents\bigo

# Проверь, что Git инициализирован
git status

# Если Git не инициализирован:
git init
git remote add origin https://github.com/ВАШ-USERNAME/bigo-mvp.git
git add .
git commit -m "Initial commit"
git push -u origin main
```

---

## 🎯 Чеклист деплоя

- [ ] Изменения сохранены локально
- [ ] Изменения закоммичены в Git
- [ ] Изменения отправлены на GitHub (`git push`)
- [ ] Vercel автоматически начал сборку
- [ ] Сборка завершилась успешно (статус "Ready")
- [ ] Очищен кэш браузера
- [ ] Проверена страница `/login`
- [ ] Проверена страница `/register`
- [ ] Проверена страница трансляции

---

## 💡 Совет

Если используешь Git, настрой автоматический деплой:
1. Vercel → Settings → Git
2. Подключи GitHub репозиторий
3. Каждый `git push` будет автоматически деплоить изменения

