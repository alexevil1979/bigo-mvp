# Настройка автоматического обновления через Cron

## 📋 Описание

Эта инструкция поможет настроить автоматическое обновление проекта с GitHub через cron. Скрипты будут периодически проверять наличие новых коммитов и автоматически обновлять код.

## 🚀 Шаг 1: Подготовка скриптов

### 1.1. Убедитесь, что скрипты исполняемые

На сервере выполните:

```bash
cd /ssd/www/bigo-mvp/scripts

# Сделайте скрипты исполняемыми
chmod +x auto-update.sh
chmod +x auto-update-frontend.sh
chmod +x auto-update-admin.sh
chmod +x auto-update-backend.sh
```

### 1.2. Проверьте пути в скриптах

Откройте скрипты и убедитесь, что путь `PROJECT_DIR="/ssd/www/bigo-mvp"` соответствует вашему реальному пути:

```bash
nano /ssd/www/bigo-mvp/scripts/auto-update.sh
# Проверьте строку: PROJECT_DIR="/ssd/www/bigo-mvp"
```

### 1.3. Создайте директорию для логов (если не существует)

```bash
sudo mkdir -p /var/log
sudo chmod 755 /var/log
```

---

## 🔧 Шаг 2: Настройка Cron

### 2.1. Откройте crontab для редактирования

```bash
sudo crontab -e
```

Или для конкретного пользователя (если не root):
```bash
crontab -e
```

### 2.2. Добавьте задачи в cron

#### Вариант A: Обновление всех компонентов раз в час

```cron
# Автоматическое обновление всех компонентов каждый час
0 * * * * /ssd/www/bigo-mvp/scripts/auto-update.sh all >> /var/log/bigo-auto-update-cron.log 2>&1
```

#### Вариант B: Раздельное обновление компонентов (рекомендуется)

```cron
# Обновление бэкенда каждые 30 минут
*/30 * * * * /ssd/www/bigo-mvp/scripts/auto-update-backend.sh >> /var/log/bigo-backend-update-cron.log 2>&1

# Обновление фронтенда каждый час
0 * * * * /ssd/www/bigo-mvp/scripts/auto-update-frontend.sh >> /var/log/bigo-frontend-update-cron.log 2>&1

# Обновление админки каждый час (с задержкой в 15 минут после фронтенда)
15 * * * * /ssd/www/bigo-mvp/scripts/auto-update-admin.sh >> /var/log/bigo-admin-update-cron.log 2>&1
```

#### Вариант C: Обновление только в рабочее время

```cron
# Обновление бэкенда каждые 30 минут с 8:00 до 22:00
*/30 8-22 * * * /ssd/www/bigo-mvp/scripts/auto-update-backend.sh >> /var/log/bigo-backend-update-cron.log 2>&1

# Обновление фронтенда каждый час с 8:00 до 22:00
0 8-22 * * * /ssd/www/bigo-mvp/scripts/auto-update-frontend.sh >> /var/log/bigo-frontend-update-cron.log 2>&1

# Обновление админки каждый час с 8:00 до 22:00 (с задержкой)
15 8-22 * * * /ssd/www/bigo-mvp/scripts/auto-update-admin.sh >> /var/log/bigo-admin-update-cron.log 2>&1
```

#### Вариант D: Обновление раз в день (в 3:00 ночи)

```cron
# Обновление всех компонентов каждый день в 3:00
0 3 * * * /ssd/www/bigo-mvp/scripts/auto-update.sh all >> /var/log/bigo-auto-update-cron.log 2>&1
```

### 2.3. Сохраните и выйдите

- В `nano`: `Ctrl+O`, `Enter`, `Ctrl+X`
- В `vi`: `:wq`, `Enter`

---

## 📊 Шаг 3: Проверка работы Cron

### 3.1. Проверьте, что задачи добавлены

```bash
sudo crontab -l
# или
crontab -l
```

Должны быть видны добавленные задачи.

### 3.2. Проверьте логи cron

```bash
# Логи cron системы
sudo tail -f /var/log/syslog | grep CRON

# Логи конкретных скриптов
tail -f /var/log/bigo-auto-update.log
tail -f /var/log/bigo-frontend-update.log
tail -f /var/log/bigo-admin-update.log
tail -f /var/log/bigo-backend-update.log
```

### 3.3. Тестовый запуск скрипта вручную

```bash
# Проверьте работу скрипта
/ssd/www/bigo-mvp/scripts/auto-update.sh all

# Или отдельные компоненты
/ssd/www/bigo-mvp/scripts/auto-update-frontend.sh
/ssd/www/bigo-mvp/scripts/auto-update-admin.sh
/ssd/www/bigo-mvp/scripts/auto-update-backend.sh
```

---

## 🔍 Шаг 4: Мониторинг и отладка

### 4.1. Просмотр логов обновлений

```bash
# Все логи обновлений
tail -f /var/log/bigo-*-update.log

# Конкретный лог
tail -f /var/log/bigo-frontend-update.log
```

### 4.2. Проверка статуса PM2 после обновления

```bash
pm2 status
pm2 logs nio-frontend --lines 20
pm2 logs nio-admin --lines 20
pm2 logs bigo-backend --lines 20
```

### 4.3. Если что-то пошло не так

```bash
# Проверьте последние записи в логах
tail -50 /var/log/bigo-auto-update.log

# Проверьте, что скрипты исполняемые
ls -la /ssd/www/bigo-mvp/scripts/

# Проверьте права доступа
sudo chmod +x /ssd/www/bigo-mvp/scripts/*.sh
```

---

## ⚙️ Настройка переменных окружения для Cron

### Проблема: Node.js не найден в cron

Cron может не видеть Node.js, если он установлен через NVM. Решение:

### Вариант 1: Использовать полный путь к Node.js

Отредактируйте скрипты, добавив в начало:

```bash
# В начале каждого скрипта добавьте:
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm use 18
```

Или используйте полный путь:

```bash
# Найдите путь к Node.js
which node
# Например: /root/.nvm/versions/node/v18.20.0/bin/node

# Добавьте в начало скрипта:
export PATH="/root/.nvm/versions/node/v18.20.0/bin:$PATH"
```

### Вариант 2: Использовать PM2 с полным путем

В скриптах замените `pm2` на полный путь:

```bash
# Найдите путь к PM2
which pm2
# Например: /usr/bin/pm2

# В скриптах используйте полный путь
/usr/bin/pm2 restart nio-frontend
```

---

## 📝 Пример полного crontab

```cron
# Автоматическое обновление проекта NIO
# Обновление бэкенда каждые 30 минут
*/30 * * * * /ssd/www/bigo-mvp/scripts/auto-update-backend.sh >> /var/log/bigo-backend-update-cron.log 2>&1

# Обновление фронтенда каждый час
0 * * * * /ssd/www/bigo-mvp/scripts/auto-update-frontend.sh >> /var/log/bigo-frontend-update-cron.log 2>&1

# Обновление админки каждый час (с задержкой в 15 минут)
15 * * * * /ssd/www/bigo-mvp/scripts/auto-update-admin.sh >> /var/log/bigo-admin-update-cron.log 2>&1

# Очистка старых логов раз в неделю (воскресенье в 2:00)
0 2 * * 0 find /var/log -name "bigo-*-update*.log" -mtime +30 -delete
```

---

## 🛡️ Безопасность

### Рекомендации:

1. **Используйте SSH ключи для Git** (если возможно):
   ```bash
   # Настройте SSH ключ для GitHub
   ssh-keygen -t ed25519 -C "your_email@example.com"
   # Добавьте публичный ключ в GitHub Settings → SSH Keys
   ```

2. **Ограничьте права доступа к скриптам**:
   ```bash
   chmod 750 /ssd/www/bigo-mvp/scripts/*.sh
   ```

3. **Используйте отдельного пользователя для cron** (не root, если возможно)

4. **Регулярно проверяйте логи** на наличие ошибок

---

## 🔄 Отключение автоматического обновления

Если нужно временно отключить автоматическое обновление:

```bash
# Откройте crontab
sudo crontab -e

# Закомментируйте нужные строки (добавьте # в начало)
# 0 * * * * /ssd/www/bigo-mvp/scripts/auto-update-frontend.sh
```

Или удалите задачи:
```bash
sudo crontab -e
# Удалите нужные строки
```

---

## ✅ Готово!

После настройки cron будет автоматически проверять и обновлять код с GitHub согласно расписанию.

**Проверка:**
1. Дождитесь времени запуска cron (или запустите вручную)
2. Проверьте логи: `tail -f /var/log/bigo-*-update.log`
3. Проверьте статус PM2: `pm2 status`



