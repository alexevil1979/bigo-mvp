# 🔐 Как войти в админ-панель

## 📋 Шаг 1: Создание первого администратора

### Вариант A: Через MongoDB Compass (визуально, самый простой)

1. **Скачай MongoDB Compass:**
   - Зайди на [MongoDB Compass](https://www.mongodb.com/products/compass)
   - Скачай и установи

2. **Подключись к MongoDB:**
   - Если MongoDB локально: `mongodb://localhost:27017`
   - Если MongoDB Atlas: используй Connection String из Atlas

3. **Сначала зарегистрируйся через Web приложение:**
   - Открой Web приложение (например, `https://bigo-web.vercel.app`)
   - Зарегистрируйся с email и паролем
   - **Запомни email и пароль!**

4. **В MongoDB Compass:**
   - Выбери базу данных: `streaming-mvp`
   - Открой коллекцию: `users`
   - Найди своего пользователя (по email)
   - Нажми на документ для редактирования
   - Измени поле `role` с `"user"` на `"admin"`
   - Нажми **"Update"** для сохранения

5. **Готово!** Теперь можешь войти в админку.

---

### Вариант B: Через MongoDB Shell (командная строка)

#### Если MongoDB локально на сервере:

```bash
# Подключись к серверу
ssh username@твой-ip-адрес

# Подключись к MongoDB
mongosh
# или для старых версий: mongo
```

#### Если MongoDB Atlas:

```bash
# Используй Connection String
mongosh "mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/streaming-mvp"
```

#### В MongoDB Shell выполни:

```javascript
// Выбери базу данных
use streaming-mvp

// Сначала зарегистрируйся через Web приложение, затем найди пользователя
db.users.findOne({ email: "твой-email@example.com" })

// Измени роль на admin
db.users.updateOne(
  { email: "твой-email@example.com" },
  { $set: { role: "admin" } }
)

// Проверь, что роль изменилась
db.users.findOne({ email: "твой-email@example.com" })
// Должно показать: "role": "admin"
```

---

### Вариант C: Через временный скрипт на сервере

```bash
# Подключись к серверу
ssh username@твой-ip-адрес

# Перейди в директорию backend
cd /var/www/bigo-mvp/backend

# Создай временный скрипт
sudo nano make-admin.js
```

Вставь следующий код (замени email на свой):

```javascript
const mongoose = require('mongoose');
const User = require('./models/User');
require('dotenv').config();

mongoose.connect(process.env.MONGODB_URI)
  .then(async () => {
    const email = 'твой-email@example.com'; // ЗАМЕНИ НА СВОЙ EMAIL
    
    const user = await User.findOne({ email });
    
    if (!user) {
      console.error('❌ Пользователь не найден! Сначала зарегистрируйся через Web приложение.');
      process.exit(1);
    }
    
    user.role = 'admin';
    await user.save();
    
    console.log('✅ Администратор создан!');
    console.log('Email:', user.email);
    console.log('Роль:', user.role);
    
    process.exit(0);
  })
  .catch(err => {
    console.error('❌ Ошибка:', err);
    process.exit(1);
  });
```

Сохрани (`Ctrl+X`, `Y`, `Enter`) и выполни:

```bash
node make-admin.js
```

После успешного выполнения удали скрипт:

```bash
sudo rm make-admin.js
```

---

## 🚪 Шаг 2: Вход в админ-панель

1. **Открой админ-панель:**
   - URL: `https://bigo-admin.vercel.app` (или твой кастомный домен)
   - Или локально: `http://localhost:3001`

2. **Введи учетные данные:**
   - **Email:** тот же, что использовал при регистрации
   - **Пароль:** тот же, что использовал при регистрации

3. **Нажми "Войти"**

4. **Если все правильно:**
   - Увидишь Dashboard с статистикой
   - Сможешь управлять пользователями, стримами и т.д.

---

## ❌ Решение проблем

### Ошибка: "Доступ только для администраторов"

**Причина:** У пользователя роль не `admin`

**Решение:**
1. Проверь роль в MongoDB:
   ```javascript
   db.users.findOne({ email: "твой-email@example.com" })
   ```
2. Если роль не `admin`, измени её (см. Шаг 1)
3. Попробуй войти снова

### Ошибка: "Пользователь не найден"

**Причина:** Пользователь не зарегистрирован

**Решение:**
1. Сначала зарегистрируйся через Web приложение
2. Затем измени роль на `admin` в MongoDB

### Ошибка: "Неверный email или пароль"

**Причина:** Неправильные учетные данные

**Решение:**
1. Проверь, что используешь правильный email и пароль
2. Если забыл пароль, можно сбросить через MongoDB (но лучше добавить функцию сброса пароля)

### Админка не открывается

**Причина:** Проблемы с деплоем или URL

**Решение:**
1. Проверь, что админка задеплоена на Vercel
2. Проверь URL в настройках проекта Vercel
3. Проверь логи на Vercel

---

## 🔑 Создание дополнительных администраторов

После того как первый админ создан, можно создавать новых через админ-панель:

1. Войди в админку
2. Перейди в раздел "Пользователи"
3. Найди нужного пользователя
4. Измени роль на `admin` (если есть такая функция в UI)

Или через MongoDB:

```javascript
use streaming-mvp

db.users.updateOne(
  { email: "новый-админ@example.com" },
  { $set: { role: "admin" } }
)
```

---

## 📝 Роли пользователей

В системе есть следующие роли:

- **`user`** - обычный пользователь (по умолчанию)
- **`streamer`** - стример (может создавать стримы)
- **`moderator`** - модератор (может модерировать чат и стримы)
- **`admin`** - администратор (полный доступ ко всему)

Для доступа в админ-панель нужна роль **`admin`**.

---

## ✅ Быстрая проверка

После создания администратора проверь:

```bash
# На сервере через MongoDB Shell
mongosh
use streaming-mvp
db.users.findOne({ email: "твой-email@example.com" }, { role: 1, email: 1 })
```

Должно вернуть:
```json
{
  "_id": ObjectId("..."),
  "email": "твой-email@example.com",
  "role": "admin"
}
```

Если `role: "admin"` - всё готово! Можешь входить в админку! 🎉

