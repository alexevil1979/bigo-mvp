# Функционал авторизации в стиле Bigo Live

## Обзор

Реализована полная система авторизации, аналогичная Bigo Live, включающая:
- QR-код для входа через мобильное приложение
- OAuth авторизацию через социальные сети
- Обновленный дизайн страниц входа и регистрации
- Страница трансляции в стиле Bigo Live

---

## 1. QR-код авторизация

### Функционал

**Компонент:** `frontend/web/components/QRCodeLogin.js`

**Описание:**
- Генерирует уникальный QR-код при загрузке страницы
- Создает временную сессию для входа (действительна 5 минут)
- Автоматически обновляет QR-код каждые 60 секунд для безопасности
- Слушает события от мобильного приложения через Socket.IO
- При сканировании QR-кода автоматически авторизует пользователя

**Как это работает:**

1. **Генерация QR-кода:**
   - Пользователь открывает страницу входа
   - Фронтенд отправляет запрос на `/api/auth/qr-session`
   - Бэкенд генерирует уникальный `sessionId`
   - Создается QR-код с данными сессии
   - QR-код отображается на странице

2. **Сканирование QR-кода:**
   - Пользователь открывает мобильное приложение BIGO LIVE
   - Находит функцию "Сканировать QR-код"
   - Сканирует QR-код с веб-страницы
   - Мобильное приложение отправляет подтверждение на бэкенд

3. **Авторизация:**
   - Бэкенд проверяет валидность сессии
   - Генерирует JWT токен для веб-приложения
   - Отправляет токен и данные пользователя через Socket.IO
   - Веб-страница получает токен и автоматически авторизует пользователя

**API Endpoints:**

- `POST /api/auth/qr-session` - Создание новой QR-сессии
  - Возвращает: `{ sessionId, qrData (base64), expiresIn }`

- `POST /api/auth/qr-confirm` - Подтверждение входа через QR (вызывается мобильным приложением)
  - Тело запроса: `{ sessionId, userId, token }`
  - Отправляет данные через Socket.IO событие `qr-login-${sessionId}`

- `GET /api/auth/qr-session/:sessionId` - Проверка статуса сессии
  - Возвращает: `{ status, expiresAt }`

**Бэкенд:** `backend/controllers/qrController.js`

**Особенности:**
- Сессии хранятся в памяти (в продакшене рекомендуется использовать Redis)
- Автоматическая очистка истекших сессий каждые 5 минут
- Защита от повторного использования сессии
- Валидация времени жизни сессии

---

## 2. OAuth авторизация

### Функционал

**Провайдеры:**
- Facebook
- Google
- VK (ВКонтакте)
- Twitter

**Как это работает:**

1. **Инициация OAuth:**
   - Пользователь нажимает кнопку социальной сети на странице входа
   - Происходит редирект на страницу авторизации провайдера
   - Пользователь авторизуется в социальной сети

2. **Callback обработка:**
   - Социальная сеть перенаправляет на `/api/auth/oauth/:provider/callback`
   - Бэкенд получает код авторизации
   - Обменивает код на access token
   - Получает данные пользователя из API провайдера

3. **Создание/обновление пользователя:**
   - Проверяется, существует ли пользователь с данным OAuth ID
   - Если нет - создается новый пользователь
   - Если да - обновляется информация (аватар, имя и т.д.)
   - Генерируется JWT токен
   - Пользователь авторизуется

**API Endpoints:**

- `GET /api/auth/oauth/:provider` - Инициация OAuth (редирект на провайдера)
- `GET /api/auth/oauth/:provider/callback` - Callback от провайдера

**Бэкенд:** `backend/controllers/oauthController.js` (требуется реализация)

**Необходимые зависимости:**
```bash
npm install passport passport-facebook passport-google-oauth20 passport-vkontakte passport-twitter
```

**Переменные окружения:**
```env
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
VK_APP_ID=your_vk_app_id
VK_APP_SECRET=your_vk_app_secret
TWITTER_CONSUMER_KEY=your_twitter_key
TWITTER_CONSUMER_SECRET=your_twitter_secret
```

---

## 3. Обновленный дизайн страниц входа

### Страница входа (`frontend/web/pages/login.js`)

**Элементы дизайна:**

1. **QR-код секция:**
   - Заголовок "Отсканируйте код чтобы войти"
   - QR-код с индикатором статуса
   - Инструкции по использованию
   - Скриншоты мобильного приложения (заглушки)

2. **Разделитель "OR":**
   - Визуальный разделитель между QR-кодом и социальными сетями

3. **Кнопки социальных сетей:**
   - Facebook (синий круг с белой буквой 'f')
   - Google (белый круг с цветным 'G')
   - VK (синий круг с белым 'VK')
   - Twitter (голубой круг с белой птицей)

4. **Форма входа:**
   - Email поле
   - Пароль поле
   - Кнопка "Войти"
   - Ссылка на регистрацию

5. **Ссылка на регистрацию:**
   - "Еще не присоединились?"
   - "Зарегистрироваться сейчас"

6. **Чекбокс согласия:**
   - "Я прочитал и согласен Соглашение пользователя, Политика конфиденциальности"
   - Обязателен для регистрации

7. **Кнопки скачивания приложения:**
   - App Store
   - Google Play

### Страница регистрации (`frontend/web/pages/register.js`)

**Аналогичный дизайн с формой регистрации:**
- Email
- Никнейм
- Пароль
- Подтверждение пароля
- Чекбокс согласия с правилами

---

## 4. Страница трансляции в стиле Bigo Live

### Компонент: `frontend/web/pages/stream/[id].js`

**Элементы дизайна:**

1. **Верхняя панель с профилем стримера:**
   - **Аватар стримера:** Круглое изображение (60x60px)
   - **Имя стримера:** С зеленым индикатором онлайн статуса
   - **BIGO ID:** Отображается ID стримера
   - **Статистика:**
     - 💰 Монеты (beans)
     - 💬 Комментарии (viewerCount)
     - 👁️ Зрители (viewerCount)
   - **Кнопки действий:**
     - "Поделиться" - копирует ссылку или открывает нативный share
     - "+ Подписаться" / "✓ Подписка" - переключатель подписки

2. **Основной видеоплеер:**
   - Полноэкранный видеоплеер
   - Градиентный оверлей справа с логотипом "BIGO LIVE" и ID стрима
   - Автоматическое воспроизведение
   - Контролы видео

3. **Приветственное сообщение:**
   - "Наслаждайся прямыми эфирами"

4. **Предупреждение о правилах:**
   - Желтый блок с предупреждением о запрещенных действиях
   - Список правил трансляции

5. **Нижняя панель с чатом:**
   - Поле ввода "Общаться со всеми"
   - Кнопка "Отправить"
   - Для неавторизованных: кнопка "Войти для чата"

6. **Баннер с рекламой:**
   - Градиентный баннер
   - Текст: "Набор ведущих и агентов Bigo Live открыт!"
   - Подзаголовок: "Безграничные возможности, высокий доход"
   - Кнопка "Присоединиться сейчас"
   - Кнопка закрытия (×)

7. **Боковая панель:**
   - Компонент подарков (для авторизованных)
   - Компонент чата

8. **Плавающие кнопки:**
   - Иконка мобильного приложения (правый нижний угол)
   - Иконка избранного (правый нижний угол)

### Функционал

**Подписка на стримера:**
- Переключатель состояния подписки
- Сохранение состояния в локальном хранилище (в продакшене - на бэкенде)

**Шаринг:**
- Использует нативный Web Share API
- Fallback на копирование в буфер обмена

**Чат:**
- Интеграция с компонентом Chat
- Отправка сообщений из нижней панели
- Синхронизация с боковой панелью чата

**Адаптивность:**
- Responsive дизайн
- На мобильных устройствах боковая панель перемещается вниз

---

## 5. Технические детали

### Зависимости

**Фронтенд:**
```json
{
  "qrcode": "^1.5.3",
  "socket.io-client": "^4.6.1"
}
```

**Бэкенд:**
```json
{
  "qrcode": "^1.5.3",
  "passport": "^0.7.0",
  "passport-facebook": "^3.0.0",
  "passport-google-oauth20": "^2.0.0",
  "passport-vkontakte": "^0.5.0",
  "passport-twitter": "^1.0.4"
}
```

### Переменные окружения

**Бэкенд (.env):**
```env
# JWT
JWT_SECRET=your_jwt_secret
JWT_EXPIRE=7d

# OAuth (опционально)
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
VK_APP_ID=your_vk_app_id
VK_APP_SECRET=your_vk_app_secret
TWITTER_CONSUMER_KEY=your_twitter_key
TWITTER_CONSUMER_SECRET=your_twitter_secret

# Socket.IO
SOCKET_URL=ws://localhost:5000
```

**Фронтенд (.env.local):**
```env
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
```

### Безопасность

1. **QR-сессии:**
   - Время жизни: 5 минут
   - Одноразовое использование
   - Автоматическая очистка истекших сессий

2. **OAuth:**
   - Валидация state параметра
   - Проверка origin для callback
   - Безопасное хранение токенов

3. **JWT:**
   - Секретный ключ в переменных окружения
   - Время жизни токена: 7 дней
   - Refresh token механизм (опционально)

---

## 6. Будущие улучшения

1. **Redis для QR-сессий:**
   - Заменить in-memory хранилище на Redis
   - Поддержка кластеризации

2. **Refresh tokens:**
   - Реализовать механизм обновления токенов
   - Улучшить безопасность

3. **Двухфакторная аутентификация:**
   - SMS/Email коды
   - TOTP (Google Authenticator)

4. **Биометрия:**
   - Face ID / Touch ID для мобильных приложений

5. **Аналитика:**
   - Отслеживание источников входа
   - Статистика использования QR-кода vs OAuth

---

## 7. Инструкции по использованию

### Для разработчиков

1. **Установка зависимостей:**
```bash
cd backend
npm install qrcode passport passport-facebook passport-google-oauth20 passport-vkontakte passport-twitter

cd ../frontend/web
npm install qrcode socket.io-client
```

2. **Настройка OAuth провайдеров:**
   - Создайте приложения в Facebook, Google, VK, Twitter
   - Получите Client ID и Client Secret
   - Добавьте в `.env` файл бэкенда

3. **Запуск:**
```bash
# Бэкенд
cd backend
npm run dev

# Фронтенд
cd frontend/web
npm run dev
```

### Для пользователей

1. **Вход через QR-код:**
   - Откройте страницу входа на веб-сайте
   - Откройте мобильное приложение BIGO LIVE
   - Найдите "Сканировать QR-код" в настройках
   - Отсканируйте QR-код
   - Автоматический вход на веб-сайт

2. **Вход через социальные сети:**
   - Нажмите на иконку социальной сети
   - Авторизуйтесь в социальной сети
   - Автоматический вход на сайт

3. **Просмотр трансляции:**
   - Откройте страницу трансляции
   - Видео автоматически воспроизводится
   - Используйте нижнюю панель для чата
   - Используйте боковую панель для подарков и расширенного чата

---

## Заключение

Реализована полная система авторизации и дизайн страниц в стиле Bigo Live. Все компоненты готовы к использованию и могут быть расширены дополнительным функционалом по мере необходимости.



